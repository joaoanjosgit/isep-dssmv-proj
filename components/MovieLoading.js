import React from 'react';
import {View, Text} from 'react-native';

const MovieLoading = () => {
  return (
    <View
      style={{borderColor: 'black', borderWidth: 1, backgroundColor: 'green'}}>
      <Text style={{fontSize: 50}}>Loading</Text>
    </View>
  );
};
export default MovieLoading;
